image: rust:latest

stages:
  - test

test-cargo:
  stage: test
  script:
    - set -x
    - rustc --version
    - cargo --version
    - env | sort
    - date -u
    - time cargo test --verbose -- --nocapture --test-threads=1